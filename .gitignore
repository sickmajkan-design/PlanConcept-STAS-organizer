# .NET
bin/
obj/
out/
*.user
*.suo
.vs/
.vscode/
*.DotSettings.user
TestResults/
artifacts/

# Logs
logs/
*.log

# Environment / secrets
.env
.env.*
!.env.example
appsettings.*.local.json
secrets.json

# Rider / IDEA
.idea/

# Node (React admin - future module)
node_modules/
dist/
build/
coverage/

# Flutter (mobile - future module)
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.packages
.pub-cache/
.pub/
*.iml
ios/Pods/
android/.gradle/

# OS
.DS_Store
Thumbs.db

# Database dumps. BACKUP_DIR defaults to ./backups, so without this the first
# run of scripts/backup.sh puts every employee record, GPS track and audit
# entry into the repository — and a dump is not something a .gitignore added
# later can take back out of the history.
/backups/
*.dump
*.dump.sha256

# Deployment environment, filled in per host. Never committed.
deploy/.env
