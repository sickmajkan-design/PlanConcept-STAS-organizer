namespace Construction.Domain.Enums;

/// <summary>Where money that is not tied to a project came from.</summary>
public enum CompanyRevenueSource
{
    VehicleRental = 1,
    ToolRental = 2,
    Other = 3,
}
