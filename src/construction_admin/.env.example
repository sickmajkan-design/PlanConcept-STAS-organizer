# Base URL of the Construction API.
VITE_API_BASE_URL=http://localhost:5000

# Google Maps JavaScript API browser key, used by the live map. Optional —
# without it the map page explains what is missing instead of failing.
VITE_GOOGLE_MAPS_API_KEY=
